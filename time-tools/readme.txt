time3 - print localtime() and gmtime() from time() every 5 seconds for up to five minutes.

time-functions - print strftime("iso-8601", localtime(...)) for
   time()
   gettimeofday()
   clock_gettime(CLOCK_REALTIME)